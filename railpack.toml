# NOTE: This file is superseded by the Dockerfile in the repo root.
# Railway will use the Dockerfile automatically when one is present,
# so this railpack.toml config is no longer active.
# It is kept here for reference only.

[options]
providers = ["python", "node"]

[packages]
nodejs = "20"
python = "3.11"
